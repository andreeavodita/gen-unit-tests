/*
FsHandle implements Closeable  public boolean down(String directory)  if (directory == null) throw new IllegalArgumentException(); Path potential = ref.resolve(directory); if (Files.isDirectory(potential) && Files.exists(potential))  movePath(potential); return true;  return false;  FsHandle(); FsHandle(String start); FsHandle(String start, FsHandleOptions options); FsHandle(FileSystem fs, String start, FsHandleOptions options); int getId(); String pwd(); void addUpdateCallback(ConsumerEV_FileListing> callback); boolean up(); boolean down(String directory); boolean setPath(String path); ListFileListlet> list(); @Override void close();
*/

@Test public void testUp() throws Exception  FsHandle fs = new FsHandle(fs, fs, fs, fs, fs, fs, fs, fs, fs, fs, fs, fs, fs, fs, fs, fs, fs, fs, fs, fs, fs); FsHandle fs = new FsHandle(false);