/*
public ListFileListlet> list() throws IOException  if (refListing == null)  try (StreamPath> paths = Files.list(ref))  refListing = paths.map(this::buildNewListlet).collect(Collectors.toList());   return refListing;
*/

@Test public void list() throws IOException  if (refListlet.class)  ListFileListlet> paths = Files.list(null);